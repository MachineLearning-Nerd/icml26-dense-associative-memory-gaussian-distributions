# Claim 3 limitations and deviations

At theorem scale the true errors are far below ordinary double
precision, so the primary numerical result is a rigorous triangle-
inequality upper certificate rather than a rounded direct distance.
The long-double checker covers representative `d=100` cases. As with
Claim 2, finite trials calibrate the theorem but do not replace its
proof-level quantifiers.
